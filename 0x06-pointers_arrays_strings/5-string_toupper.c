#include "main.h"
/**
 * string_toupper - fcn that changes all lowercase letters
 * to uppercase.
 * @s: str to be altered
 * Return: altered str
 */

char *string_toupper(char *)
{
	return ('\0');
}
